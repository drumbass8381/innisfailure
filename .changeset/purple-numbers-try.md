---
"innisfailures": patch
"frontend": patch
"processor": patch
---

- `EventBus` moved to a separate package `@innisfailures/event-bus`
- Subscription to `EventBus` events moved in one place, inside bot `Processor`
